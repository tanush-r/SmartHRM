apiVersion: v1
kind: Service
metadata:
  name: ai-assistant
  namespace: prod
spec:
  selector:
    app: ai-assistant
  ports:
  - protocol: TCP
    port: 8010
    targetPort: 8010
  type: ClusterIP # Internal communication within the cluster
